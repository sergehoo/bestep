# Dépendances de DÉVELOPPEMENT — NE PAS embarquer dans l'image runtime.
# CORRECTIF audit INFRA-19.
#
# Installation : `pip install -r requirements.txt -r requirements-dev.txt`.
# La CI utilise les deux ; la production utilise SEULEMENT requirements.txt.

# Outils qualité
ruff==0.6.9
black==25.11.0
isort==5.13.2
pre-commit==4.3.0

# Tests
pytest==8.4.2
pytest-django==4.11.1
pytest-cov==5.0.0
factory_boy==3.3.3
Faker==37.12.0

# Fixtures + tooling Django
django-extensions==4.1

# Sécurité (CVE audit)
pip-audit==2.7.3
