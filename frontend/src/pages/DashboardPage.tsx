/**
 * DashboardPage.tsx — DEPRECATED (R5).
 *
 * Cette page monolithique a été scindée en 3 pages dédiées :
 *   - StudentDashboardPage    → /dashboard
 *   - InstructorDashboardPage → /dashboard/instructor
 *   - AdminDashboardPage      → /dashboard/admin
 *
 * Ce fichier est conservé pour l'historique — il n'est plus référencé
 * par le router. Il sera supprimé en R6.
 */
export default function DashboardPage() {
  return null;
}
